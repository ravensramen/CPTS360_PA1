Compilation Instructions:
Navigate to program files, create an executable with gcc and all .c files. Run
executable with "./" command in terminal. 

gcc -Wall -Wextra -std=c99 functions.c main.c -o myproject

Assumptions:

Input and Output Examples:
